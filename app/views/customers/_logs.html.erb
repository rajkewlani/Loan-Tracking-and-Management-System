<%= display_flash(flash) %>

<table class="tiny_table">
  <% customer.logs.each do |log| %>
    <tr>
      <td nowrap><%= log.created_at.strftime("%m/%d/%y %I:%M:%S %p") %></td>
      <td class="last"><%= h log.message %></td>
    </tr>
  <% end %>
</table>